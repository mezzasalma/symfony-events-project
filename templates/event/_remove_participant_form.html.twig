<form method="post" action="{{ path('event_remove_participant', {'id': event.id}) }}">
  <input type="hidden" name="_token" value="{{ csrf_token('put' ~ event.id) }}">
  <button class="btn">Se désinscrire</button>
</form>
